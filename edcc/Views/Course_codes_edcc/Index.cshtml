@model IEnumerable<edcc.Models.Course_codes_edcc>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.edccCourse)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.course_id)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.edccCourse)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.course_id)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.edcc_Course_code_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.edcc_Course_code_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.edcc_Course_code_id })
        </td>
    </tr>
}

</table>
