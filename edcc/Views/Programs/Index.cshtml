@model IEnumerable<edcc.Models.Program>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.prog_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.prog_desc)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.prog_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.prog_desc)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.prog_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.prog_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.prog_id })
        </td>
    </tr>
}

</table>
