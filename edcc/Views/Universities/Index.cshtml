@model IEnumerable<edcc.Models.University>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.un_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.un_description)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.un_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.un_description)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.un_id }) |
            @Html.ActionLink("Details", "Details", new { id=item.un_id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.un_id })
        </td>
    </tr>
}

</table>
